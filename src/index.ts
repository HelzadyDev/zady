import * as functions from "#functions"
import { bgColors, colors, terminalStyle } from "#core"

const zady = {
  ...functions,
  
  style: {
    bgColors,
    colors,
    terminalStyle
  }
}

export default zady

